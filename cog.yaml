build:
  gpu: true
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
    - "pyipopt"
  python_version: "3.8"
  python_packages:
    - "torch==1.8.1"


train: "train.py:Trainer"

predict: "predict.py:Predictor"


cog --base_mode=6B --data_set=xxx --finetuning_type lora train --> type: checkpoint_id


cog --base_mode=6B --checkpoint_id=1001 -finetuning_type lora deploy  --> type: ias url
